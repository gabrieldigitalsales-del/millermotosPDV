RODE PRIMEIRO NO SUPABASE:

1) Abra Supabase > SQL Editor > New Query.
2) Cole TODO o conteúdo do arquivo:
   sql_reset_completo_miller_motos_final.sql
3) Clique em Run.
4) Abra o sistema novamente e pressione Ctrl+F5.

ATENCAO: esse reset apaga e recria todas as tabelas mm_pdv_*.
Ele corrige:
- colunas que não existem;
- id null/not-null;
- ON CONFLICT sem unique constraint;
- duplicate key em usuários;
- deadlock/baixa dupla por trigger antiga;
- permissões completas;
- login vendedor primeiro.
